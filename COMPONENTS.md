# Component Library Documentation\n\n## UI Components (Atoms)\n\n### Button\nVersatile button component with multiple variants and sizes.\n\n```tsx\nimport { Button } from '@/components';\n\n// Primary button\n<Button variant=\"primary\">Submit</Button>\n\n// With icon\n<Button icon={<Plus className=\"w-4 h-4\" />}>Add Item</Button>\n\n// Loading state\n<Button isLoading>Saving...</Button>\n\n// Full width\n<Button fullWidth>Full Width Button</Button>\n\n// Variants: primary, secondary, outline, danger, success, ghost, accent\n// Sizes: xs, sm, md, lg, xl\n```\n\n### Input\nForm input with label, error handling, and icon support.\n\n```tsx\nimport { Input } from '@/components';\n\n<Input\n  label=\"Email\"\n  type=\"email\"\n  placeholder=\"Enter your email\"\n  error={errors.email}\n  helperText=\"We'll never share your email\"\n  icon={<Mail className=\"w-4 h-4\" />}\n  required\n/>\n```\n\n### Card\nLayout component for grouping content.\n\n```tsx\nimport { Card, CardHeader, CardTitle, CardContent, CardFooter } from '@/components';\n\n<Card interactive onClick={() => console.log('clicked')}>\n  <CardHeader>\n    <CardTitle>Card Title</CardTitle>\n  </CardHeader>\n  <CardContent>\n    Card content goes here\n  </CardContent>\n  <CardFooter>\n    Footer content\n  </CardFooter>\n</Card>\n```\n\n### Badge\nStatus or tag display component.\n\n```tsx\nimport { Badge } from '@/components';\n\n// Variants: default, primary, success, warning, danger, info, purple, orange, teal, indigo, emerald\n<Badge variant=\"success\">Active</Badge>\n<Badge variant=\"danger\">Failed</Badge>\n```\n\n### Modal\nDialog/Modal component for overlays and forms.\n\n```tsx\nimport { Modal } from '@/components';\n\nconst [isOpen, setIsOpen] = useState(false);\n\n<Modal\n  isOpen={isOpen}\n  onClose={() => setIsOpen(false)}\n  title=\"Confirm Action\"\n  size=\"md\"\n  actions={\n    <>\n      <Button onClick={() => setIsOpen(false)}>Cancel</Button>\n      <Button variant=\"primary\">Confirm</Button>\n    </>\n  }\n>\n  Modal content here\n</Modal>\n```\n\n### Spinner\nLoading indicator.\n\n```tsx\nimport { Spinner, LoadingOverlay, SkeletonLoader } from '@/components';\n\n// Spinner\n<Spinner size=\"md\" color=\"primary\" />\n\n// Full screen loading overlay\n<LoadingOverlay />\n\n// Skeleton loader\n<SkeletonLoader className=\"h-8 w-32\" />\n```\n\n### DataTable\nAdvanced table with sorting, pagination, and selection.\n\n```tsx\nimport { DataTable, Column } from '@/components';\n\nconst columns: Column<Customer>[] = [\n  { key: 'name', header: 'Name', sortable: true },\n  { key: 'email', header: 'Email', render: (val) => <a href={`mailto:${val}`}>{val}</a> },\n];\n\n<DataTable\n  columns={columns}\n  data={customers}\n  onEdit={handleEdit}\n  onDelete={handleDelete}\n  pageSize={10}\n  selectable\n  onSelectRows={handleSelect}\n/>\n```\n\n---\n\n## Layout Components\n\n### MainLayout\nComplete page layout with sidebar and header.\n\n```tsx\nimport { MainLayout } from '@/components';\n\n<MainLayout title=\"Page Title\">\n  <div>Your page content here</div>\n</MainLayout>\n```\n\n### ModernSidebar\nNavigation sidebar with role-based menu items.\n\n```tsx\nimport { ModernSidebar } from '@/components';\n\n<ModernSidebar isOpen={true} onClose={() => {}} />\n```\n\n### ModernHeader\nSticky header with search and notifications.\n\n```tsx\nimport { ModernHeader } from '@/components';\n\n<ModernHeader\n  onMenuClick={toggleSidebar}\n  title=\"Dashboard\"\n/>\n```\n\n---\n\n## Dashboard Components\n\n### KPICard\nKey Performance Indicator card with trend indicators.\n\n```tsx\nimport { KPICard } from '@/components';\n\n<KPICard\n  title=\"Total Customers\"\n  value={124}\n  icon={<Users className=\"w-6 h-6\" />}\n  color=\"blue\"\n  change={{ value: 12, isPositive: true }}\n  onClick={() => navigate('/customers')}\n/>\n```\n\n### AdminDashboard\nComplete admin dashboard with KPIs, charts, and activity feed.\n\n```tsx\nimport { AdminDashboard } from '@/components';\n\n<AdminDashboard\n  stats={{\n    totalCustomers: 124,\n    totalProjects: 45,\n    totalQuotations: 32,\n    completedProjects: 28,\n    pendingProjects: 17,\n    revenueThisMonth: 450000,\n  }}\n  recentProjects={projects}\n  recentLeads={leads}\n/>\n```\n\n### ManagerDashboard\nManager-specific dashboard with assigned projects and team.\n\n```tsx\nimport { ManagerDashboard } from '@/components';\n\n<ManagerDashboard\n  stats={{\n    assignedProjects: 12,\n    completedThisMonth: 8,\n    pendingTasks: 5,\n    assignedWorkers: 8,\n  }}\n  assignedProjects={projects}\n  workers={teamMembers}\n/>\n```\n\n### WorkerDashboard\nWorker-specific dashboard with daily jobs and statistics.\n\n```tsx\nimport { WorkerDashboard } from '@/components';\n\n<WorkerDashboard\n  stats={{\n    todayJobs: 3,\n    completedJobs: 142,\n    totalHours: 856,\n    rating: 4.8,\n  }}\n  todayJobs={jobs}\n  completedJobs={pastJobs}\n/>\n```\n\n### CustomerDashboard\nCustomer portal dashboard with project status and quotations.\n\n```tsx\nimport { CustomerDashboard } from '@/components';\n\n<CustomerDashboard\n  stats={{\n    activeProjects: 2,\n    completedProjects: 5,\n    totalSpent: 150000,\n    warranty: 3,\n  }}\n  projects={myProjects}\n  quotations={myQuotations}\n/>\n```\n\n---\n\n## Entity Management Components\n\n### CustomerList & CustomerForm\nCustomer management with search and CRUD operations.\n\n```tsx\nimport { CustomerList, CustomerForm } from '@/components';\n\nconst [isFormOpen, setIsFormOpen] = useState(false);\nconst [selectedCustomer, setSelectedCustomer] = useState<Customer | null>(null);\n\n<>\n  <CustomerList\n    customers={customers}\n    onEdit={(customer) => {\n      setSelectedCustomer(customer);\n      setIsFormOpen(true);\n    }}\n    onCreate={() => {\n      setSelectedCustomer(null);\n      setIsFormOpen(true);\n    }}\n  />\n  <CustomerForm\n    isOpen={isFormOpen}\n    onClose={() => setIsFormOpen(false)}\n    customer={selectedCustomer}\n    onSubmit={handleSave}\n  />\n</>\n```\n\n### ProjectList\nProject listing with status filtering and management.\n\n```tsx\nimport { ProjectList } from '@/components';\n\n<ProjectList\n  projects={projects}\n  onEdit={handleEditProject}\n  onDelete={handleDeleteProject}\n  onCreate={handleCreateProject}\n/>\n```\n\n### LeadList\nLead management with conversion tracking.\n\n```tsx\nimport { LeadList } from '@/components';\n\n<LeadList\n  leads={leads}\n  onEdit={handleEdit}\n  onDelete={handleDelete}\n  onConvert={handleConvert}\n  onCreate={handleCreate}\n/>\n```\n\n### QuotationList\nQuotation management with PDF export.\n\n```tsx\nimport { QuotationList } from '@/components';\n\n<QuotationList\n  quotations={quotations}\n  onEdit={handleEdit}\n  onDelete={handleDelete}\n  onPreview={handlePreview}\n  onExport={handleExport}\n  onCreate={handleCreate}\n/>\n```\n\n### WorkerList\nWorker/Team member management.\n\n```tsx\nimport { WorkerList } from '@/components';\n\n<WorkerList\n  workers={workers}\n  onEdit={handleEdit}\n  onDelete={handleDelete}\n  onCreate={handleCreate}\n/>\n```\n\n---\n\n## Shared Components\n\n### PageHeader\nPage title and action bar.\n\n```tsx\nimport { PageHeader } from '@/components';\n\n<PageHeader\n  title=\"Customers\"\n  description=\"Manage your customers\"\n  icon={<Users />}\n  breadcrumbs={[\n    { label: 'Dashboard', href: '/' },\n    { label: 'Customers' },\n  ]}\n  actions={\n    <Button variant=\"primary\">Add Customer</Button>\n  }\n/>\n```\n\n### EmptyState\nEmpty state placeholder.\n\n```tsx\nimport { EmptyState } from '@/components';\n\n<EmptyState\n  icon={<Package />}\n  title=\"No products found\"\n  description=\"Start by adding your first product\"\n  action={{\n    label: 'Add Product',\n    onClick: () => handleCreate(),\n  }}\n/>\n```\n\n### ConfirmDialog\nConfirmation dialog for destructive actions.\n\n```tsx\nimport { ConfirmDialog } from '@/components';\n\nconst [isOpen, setIsOpen] = useState(false);\n\n<ConfirmDialog\n  isOpen={isOpen}\n  title=\"Delete Customer\"\n  description=\"This action cannot be undone. Are you sure?\"\n  isDangerous={true}\n  onConfirm={handleDelete}\n  onCancel={() => setIsOpen(false)}\n  confirmLabel=\"Delete\"\n/>\n```\n\n---\n\n## Design System\n\n### Colors\n- Primary: `#1a3a6b` (Navy)\n- Accent: `#f97316` (Orange)\n- Success: `#16a34a` (Green)\n- Warning: `#ea580c` (Yellow/Orange)\n- Error: `#dc2626` (Red)\n- Background: `#f9fafb` (Light Gray)\n- Text: `#1f2937` (Dark)\n\n### Typography\n- Headlines: Inter Bold\n- Body: System fonts\n\n### Spacing Scale\n- 2px, 4px, 6px, 8px, 12px, 16px, 20px, 24px, 32px\n\n---\n\n## Best Practices\n\n1. **Use TypeScript** - All components are fully typed\n2. **Compose Components** - Build complex UIs from simple components\n3. **Props Documentation** - Always provide clear prop descriptions\n4. **Error Handling** - Use error states from forms and data tables\n5. **Loading States** - Always show loading indicators for async operations\n6. **Accessibility** - All components include ARIA labels and semantic HTML\n7. **Responsive Design** - All components are mobile-first and responsive\n8. **Performance** - Components use React.forwardRef for ref forwarding\n\n---\n\n## Customization\n\nAll components use Tailwind CSS for styling and can be customized via className prop.\n\n```tsx\n<Button className=\"rounded-none uppercase tracking-widest\">\n  Custom Button\n</Button>\n```\n\n---\n\n**Built with ❤️ for ManageNewTech**\n"